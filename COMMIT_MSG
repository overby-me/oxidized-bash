fix(rust/bash): Add comsub expansion in assoc subscripts and typeset error prefix

Additional Phase 45 improvements:

- Pre-expand $(...) command substitutions inside associative array
  subscripts in expand_param before lookup, fixing ${A[$(echo Darwin)]}
  which was returning empty instead of looking up the key. This fixes
  assoc16.sub stderr output and value lookups.
- Use current_builtin name (typeset/declare/local) in all declare
  builtin error messages instead of hardcoded "declare", matching bash
  behavior when invoked as typeset.

assoc ~155->~153, nameref ~626->~589 nix diff lines